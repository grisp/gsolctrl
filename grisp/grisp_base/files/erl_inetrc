%--- Erlang Inet Configuration --------------------------------------------

% Add hosts
{host, {192,168,90,6}, ["yrael"]}.

% Do not monitor the hosts file
{hosts_file, ""}.

% Disable caching
{cache_size, 0}.

% Specify lookup method
{lookup, [file, native]}.
